import styled from 'styled-components';
import ContainerItem from '../../components/ContainerItem';
import { fontSizes } from '../../styles/types';

export default styled(ContainerItem)`
  ${fontSizes.medium};
  text-align: right;
`;
